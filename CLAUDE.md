# QuenchWorks website

The agent instructions for this repo live in **@AGENTS.md** — read it first. It covers the
stack, commands (`pnpm dev` / `build` / `sync` / `scan`), the generated-data rules, i18n,
accessibility, and the hard rules (validate before commit, the `Co-Authored-By: QuenchWorks
<info@quench-works.com>` trailer, keep `astro check` at 0/0/0).

## Design Context

- **@PRODUCT.md** — who/what/why (register: `brand`; skeptical platform-engineer users;
  "Technical, precise, trustworthy"; anti-references; strategic principles).
- **@DESIGN.md** — the visual system: monochrome dark tokens, Inter + JetBrains Mono,
  flat/border-not-shadow, functional-color-only, and the absolute bans.

Keep this file thin: put durable instructions in `AGENTS.md`, not here.
