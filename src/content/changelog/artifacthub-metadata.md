---
title: ArtifactHub categories + verified-publisher metadata across all charts
date: 2026-06-07
summary: Every chart now carries ArtifactHub category and verified-publisher metadata, so the catalog is easier to find and the publisher is confirmed.
tags: ["charts", "artifacthub", "metadata"]
---

All charts now publish complete ArtifactHub metadata. Each chart declares its ArtifactHub category, so the catalog sorts and filters correctly on ArtifactHub, and carries the verified-publisher annotation that confirms QuenchWorks as the source.

The result is that searching ArtifactHub for the org returns the full set of charts with consistent categories and a verified badge, matching how the catalog is grouped on this site.
